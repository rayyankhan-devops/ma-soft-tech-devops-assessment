apiVersion: v1
kind: Service
metadata:
  name: ma-devops-backend-live
  namespace: ma-devops
  labels:
    app: ma-devops-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: ma-devops-backend
    version: blue
